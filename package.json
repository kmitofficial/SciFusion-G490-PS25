{
  "name": "scifusion",
  "version": "2.0.0",
  "lockfileVersion": 3,
  "requires": true,
  "description": "Full-stack research automation system",
  "packages": {},
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && npm run build",
    "start": "concurrently \"cd frontend && npm start\" \"cd backend && uvicorn server.main:app --host 0.0.0.0 --port 8000\""
  }
}
